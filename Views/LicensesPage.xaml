<Page
    x:Class="FluentScrobbler.Views.LicensesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentScrobbler"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="BackButton"
                        Click="BackButton_Click"
                        Padding="8"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <ic:SymbolIcon Symbol="ArrowLeft" FontSize="16" />
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Open Source Licenses" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
                    <TextBlock Text="Third-party open-source libraries and components used in Fluent Scrobbler." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="Fluent Scrobbler" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="GPL-3.0 License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/snw-mint/fluent-scrobbler" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="Windows App SDK &amp; WinUI 3" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="MIT License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/microsoft/WindowsAppSDK" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="FluentIcons.WinUI" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="MIT License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/davidxuang/FluentIcons" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="H.NotifyIcon" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="MIT License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/HavenDV/H.NotifyIcon" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="sqlite-net" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Apache-2.0 License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/praeclarum/sqlite-net" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="System.Security.Cryptography.ProtectedData" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="MIT License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/dotnet/runtime" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="Microsoft.Windows.SDK.BuildTools" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Microsoft Software License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="NuGet" NavigateUri="https://www.nuget.org/packages/Microsoft.Windows.SDK.BuildTools" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="Microsoft.Windows.SDK.BuildTools.WinApp" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Microsoft Software License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="NuGet" NavigateUri="https://www.nuget.org/packages/Microsoft.Windows.SDK.BuildTools.WinApp" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>

                    <MenuFlyoutSeparator />

                    <Grid>
                        <StackPanel Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text=".NET Runtime &amp; Libraries" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="MIT License" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/dotnet/runtime" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
