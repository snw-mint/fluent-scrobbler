<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Fluent Scrobbler.Views.ContributePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluent Scrobbler"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="36">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Center">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12" Padding="24">
                <Grid ColumnSpacing="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Source="ms-appx:///Assets/contribute.png" Width="72" Height="72" Stretch="Uniform" VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" Spacing="6" VerticalAlignment="Center">
                        <TextBlock Text="Contribute to Fluent Scrobbler" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
                        <TextBlock Text="Fluent Scrobbler is free and open-source. Whether you report bugs, share ideas, contribute code, or buy me a coffee, every bit helps fuel continuous development." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Spacing="12">
                <TextBlock Text="Support" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ic:SymbolIcon Grid.Column="0" Symbol="DrinkCoffee" FontSize="24" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="Buy Me a Coffee" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="If you love using Fluent Scrobbler, consider supporting development with a small donation." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>

                        <Button Grid.Column="2" Click="ExternalLink_Click" Tag="https://buymeacoffee.com/snw.mint" VerticalAlignment="Center">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Support Now" />
                                    <ic:SymbolIcon Symbol="Open" FontSize="14" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Report an Issue" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ic:SymbolIcon Grid.Column="0" Symbol="Bug" FontSize="24" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="Report a Bug" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Experiencing a scrobble glitch or app crash? Open an issue on GitHub to help us fix it." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>

                        <Button Grid.Column="2" Click="ExternalLink_Click" Tag="https://github.com/snw-mint/fluent-scrobbler/issues/new?template=bug_report.yml" VerticalAlignment="Center">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Report Issue" />
                                    <ic:SymbolIcon Symbol="Open" FontSize="14" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Feature Requests" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ic:SymbolIcon Grid.Column="0" Symbol="Lightbulb" FontSize="24" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="Suggest a Feature" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Have an idea future releases? Share your feedback with the community." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>

                        <Button Grid.Column="2" Click="ExternalLink_Click" Tag="https://github.com/snw-mint/fluent-scrobbler/issues/new?template=feature_request.yml" VerticalAlignment="Center">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Submit Request" />
                                    <ic:SymbolIcon Symbol="Open" FontSize="14" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Source Code &amp; Community" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ic:SymbolIcon Grid.Column="0" Symbol="Code" FontSize="24" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="GitHub Repository" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Star the repository, inspect the code, or submit a Pull Request to help improve the app." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>

                        <Button Grid.Column="2" Click="ExternalLink_Click" Tag="https://github.com/snw-mint/fluent-scrobbler" VerticalAlignment="Center">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="View Repository" />
                                    <ic:SymbolIcon Symbol="Open" FontSize="14" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
