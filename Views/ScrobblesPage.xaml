<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FluentScrobbler.Views.ScrobblesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentScrobbler"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ic="using:FluentIcons.WinUI"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,24" SizeChanged="ScrollViewer_SizeChanged">
        <StackPanel x:Name="MainContentPanel" Spacing="20" HorizontalAlignment="Stretch">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Scrobbles" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
                    <TextBlock Text="Your recent listening history and current playing track" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
                <Button x:Name="RefreshButton"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        ToolTipService.ToolTip="Refresh scrobbles"
                        Click="RefreshButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ic:SymbolIcon x:Name="RefreshIcon" Symbol="ArrowSync" FontSize="16" />
                        <ProgressRing x:Name="RefreshRing" Width="14" Height="14" IsActive="False" Visibility="Collapsed" />
                        <TextBlock Text="Refresh" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <StackPanel x:Name="NowPlayingPanel" Spacing="12">
                <TextBlock Text="Now Playing" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />

                <Border x:Name="NowPlayingCard" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                    <Grid ColumnSpacing="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="80" Height="80" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}">
                            <Grid>
                                <ic:SymbolIcon x:Name="NowPlayingFallbackIcon" Symbol="MusicNote1" FontSize="32" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Collapsed" />
                                <ic:SymbolIcon x:Name="NowPlayingIdleIcon" Symbol="Snooze" FontSize="32" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <Image x:Name="NowPlayingAlbumArtImage" Width="80" Height="80" Stretch="UniformToFill" Visibility="Collapsed" />
                            </Grid>
                        </Border>

                        <StackPanel x:Name="NowPlayingActiveContainer" Grid.Column="1" VerticalAlignment="Center" Spacing="6" Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="6,2">
                                    <TextBlock Text="NOW PLAYING" FontSize="10" FontWeight="Bold" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                </Border>
                                <TextBlock x:Name="NowPlayingSourceText" Text="Last.fm" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock x:Name="NowPlayingTrackText" Text="Track Title" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                            <TextBlock x:Name="NowPlayingArtistAlbumText" Text="Artist • Album" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <StackPanel x:Name="NowPlayingIdleContainer" Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="Silence is golden... but music is better" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="Play something to start scrobbling." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <Button x:Name="NowPlayingLikeButton"
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"
                                Style="{ThemeResource SubtleButtonStyle}"
                                ToolTipService.ToolTip="Love track"
                                Padding="8"
                                Click="NowPlayingLikeButton_Click">
                            <ic:SymbolIcon x:Name="NowPlayingLikeIcon" Symbol="Heart" IconVariant="Regular" FontSize="20" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Recent History" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />

                <StackPanel x:Name="LoadingContainer" Visibility="Collapsed" HorizontalAlignment="Center" Spacing="12" Margin="0,16,0,16">
                    <ProgressRing x:Name="LoadingRing" IsActive="False" Width="32" Height="32" HorizontalAlignment="Center" />
                    <TextBlock Text="Loading scrobbles..." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>

                <StackPanel x:Name="ScrobblesItemsContainer" Spacing="8" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
