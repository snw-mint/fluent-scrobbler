<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FluentScrobbler.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentScrobbler"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ic="using:FluentIcons.WinUI"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="36">
        <StackPanel Spacing="24">
            <StackPanel Spacing="4">
                <TextBlock Text="Account" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
                <TextBlock Text="Manage your profile settings and scrobbler integrations" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12" Padding="28">
                <Grid ColumnSpacing="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="96" Height="96" CornerRadius="48" Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <Grid>
                            <FontIcon x:Name="UserAvatarIcon" Glyph="&#xE77B;" FontSize="44" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <Image x:Name="UserAvatarImage" Width="96" Height="96" Stretch="UniformToFill" Visibility="Collapsed" />
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Name="AccountTitleText" Text="Login to Lastfm Account" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
                            <Border x:Name="SupporterBadge" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Visibility="Collapsed">
                                <TextBlock Text="Supporter" FontSize="10" FontWeight="Bold" Foreground="White" />
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="AccountSubtitleText" Text="Not connected" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="AccountDetailsText" Text="" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Collapsed" />
                    </StackPanel>

                    <Button x:Name="ActionButton" Grid.Column="2" VerticalAlignment="Center" Style="{ThemeResource AccentButtonStyle}" Padding="20,10" Click="OnActionButtonClick">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon x:Name="ActionButtonIcon" Glyph="&#xE8A7;" FontSize="16" />
                            <TextBlock x:Name="ActionButtonText" Text="Login" FontSize="14" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <StackPanel x:Name="AccountOptionsSection" Spacing="12" Visibility="Collapsed">
                <TextBlock Text="Account Options" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                    <StackPanel Spacing="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock Text="View Last.fm Profile" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="Open your Last.fm profile page in browser" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <Button Grid.Column="1" Click="ViewProfile_Click">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <ic:SymbolIcon Symbol="Open" FontSize="14" />
                                    <TextBlock Text="View Profile" />
                                </StackPanel>
                            </Button>
                        </Grid>

                        <MenuFlyoutSeparator />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock Text="Sync Manually" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock x:Name="SyncStatusText" Text="Force a data refresh (available once per minute)" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <Button x:Name="SyncButton" Grid.Column="1" Click="SyncManually_Click">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <ic:SymbolIcon Symbol="ArrowClockwise" FontSize="14" />
                                    <TextBlock x:Name="SyncButtonText" Text="Sync Now" />
                                </StackPanel>
                            </Button>
                        </Grid>

                        <MenuFlyoutSeparator />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock Text="Import &amp; Export Settings" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="Backup or restore your application configuration" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <Button Click="ExportSettings_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <ic:SymbolIcon Symbol="ArrowDownload" FontSize="14" />
                                        <TextBlock Text="Export" />
                                    </StackPanel>
                                </Button>
                                <Button Click="ImportSettings_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <ic:SymbolIcon Symbol="ArrowUpload" FontSize="14" />
                                        <TextBlock Text="Import" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
